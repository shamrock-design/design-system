# Shamrock packages are published to GitHub Packages under the org scope.
# NOTE: GitHub Packages requires the npm scope to equal the GitHub org name.
# If the org is not "shamrock", update the scope here and in every package.json.
@shamrock:registry=https://npm.pkg.github.com
