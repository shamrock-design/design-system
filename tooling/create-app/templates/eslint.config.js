import shamrock from "@shamrock-design/eslint-config";

// The Shamrock canon (no hex, no magic px) + a React/TypeScript baseline.
// Add project-specific tweaks by spreading: [...shamrock, { rules: { … } }].
export default shamrock;
